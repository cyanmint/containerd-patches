diff --git a/pkg/shim/shim.go b/pkg/shim/shim.go
index 2f93203..bea2388 100644
--- a/pkg/shim/shim.go
+++ b/pkg/shim/shim.go
@@ -33,6 +33,7 @@ import (
 	shimapi "github.com/containerd/containerd/api/runtime/task/v3"
 	"github.com/containerd/containerd/api/types"
 	"github.com/containerd/containerd/v2/core/events"
+	"github.com/containerd/containerd/v2/defaults"
 	"github.com/containerd/containerd/v2/pkg/namespaces"
 	"github.com/containerd/containerd/v2/pkg/protobuf"
 	"github.com/containerd/containerd/v2/pkg/protobuf/proto"
@@ -127,6 +128,7 @@ var (
 	bundlePath           string
 	addressFlag          string
 	containerdBinaryFlag string
+	prefixFlag           string
 	action               string
 )
 
@@ -158,9 +160,16 @@ func parseFlags() {
 	flag.StringVar(&containerdBinaryFlag, "publish-binary", "",
 		fmt.Sprintf("path to publish binary (used for publishing events), but %s will ignore this flag, please use the %s env", os.Args[0], ttrpcAddressEnv),
 	)
+	flag.StringVar(&prefixFlag, "prefix", "", "prefix directory for all default paths")
 
 	flag.Parse()
 	action = flag.Arg(0)
+
+	// Set PathPrefix before any default-path computation so all subsystems
+	// (including process.RuncRoot usage) see the correct prefix.
+	if prefixFlag != "" {
+		defaults.PathPrefix = prefixFlag
+	}
 }
 
 func setRuntime() {
diff --git a/pkg/shim/util.go b/pkg/shim/util.go
index 674c257..07dcb7d 100644
--- a/pkg/shim/util.go
+++ b/pkg/shim/util.go
@@ -32,6 +32,7 @@ import (
 	"github.com/containerd/ttrpc"
 	"github.com/containerd/typeurl/v2"
 
+	"github.com/containerd/containerd/v2/defaults"
 	"github.com/containerd/containerd/v2/pkg/atomicfile"
 	"github.com/containerd/containerd/v2/pkg/namespaces"
 	"github.com/containerd/containerd/v2/pkg/protobuf/proto"
@@ -64,6 +65,9 @@ func Command(ctx context.Context, config *CommandConfig) (*exec.Cmd, error) {
 		"-address", config.Address,
 		"-publish-binary", self,
 	}
+	if defaults.PathPrefix != "" {
+		args = append(args, "-prefix", defaults.PathPrefix)
+	}
 	args = append(args, config.Args...)
 	cmd := exec.CommandContext(ctx, config.Runtime, args...)
 	cmd.Dir = config.Path
diff --git a/pkg/shim/util_unix.go b/pkg/shim/util_unix.go
index 874aaf5..094c959 100644
--- a/pkg/shim/util_unix.go
+++ b/pkg/shim/util_unix.go
@@ -73,7 +73,11 @@ func AdjustOOMScore(pid int) error {
 	return nil
 }
 
-const socketRoot = defaults.DefaultStateDir
+// socketRoot returns the effective default state directory for shim sockets,
+// accounting for any PathPrefix set via the --prefix flag.
+func socketRoot() string {
+	return defaults.Prefix(defaults.DefaultStateDir)
+}
 
 // SocketAddress returns a socket address
 func SocketAddress(ctx context.Context, socketPath, id string, debug bool) (string, error) {
@@ -86,7 +90,7 @@ func SocketAddress(ctx context.Context, socketPath, id string, debug bool) (stri
 		path = filepath.Join(path, "debug")
 	}
 	d := sha256.Sum256([]byte(path))
-	return fmt.Sprintf("unix://%s/%x", filepath.Join(socketRoot, "s"), d), nil
+	return fmt.Sprintf("unix://%s/%x", filepath.Join(socketRoot(), "s"), d), nil
 }
 
 // AnonDialer returns a dialer for a socket
